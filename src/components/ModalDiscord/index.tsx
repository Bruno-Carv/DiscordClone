import React from 'react';

import { Container, Modal, ModalContent, ModalHeader, ModalBody, ModalFooter } from './styles';

const LoadDiscrod: React.FC = () => {
    return (
        <Container>
            <Modal>
                <ModalContent>
                    <ModalHeader>

                    </ModalHeader>
                    <ModalBody>
                        
                    </ModalBody>
                    <ModalFooter>

                    </ModalFooter>
                </ModalContent>
            </Modal>
        </Container>
    );
};

export default LoadDiscrod;